 import { SignJWT, jwtVerify } from "jose"; function getSecret(): Uint8Array { const secretValue = process.env.SESSION_SECRET; if (!secretValue || secretValue.length < 32) { throw new Error( "SESSION_SECRET is missing or too short. Set a random 32+ character value in your environment before starting the app." ); } return new TextEncoder().encode(secretValue); } export type SessionPayload = { userId: string; email: string; role: string; permissions: string[]; }; const SESSION_COOKIE = "ahm_session"; const SESSION_TTL_SECONDS = 60 * 60 * 8; export async function createSessionToken(payload: SessionPayload): Promise<string> { return new SignJWT(payload as any) .setProtectedHeader({ alg: "HS256" }) .setIssuedAt() .setExpirationTime(`${SESSION_TTL_SECONDS}s`) .sign(getSecret()); } export async function verifySessionToken(token: string): Promise<SessionPayload | null> { try { const { payload } = await jwtVerify(token, getSecret()); return payload as unknown as SessionPayload; } catch { return null; } } export const sessionCookieName = SESSION_COOKIE; export const sessionMaxAgeSeconds = SESSION_TTL_SECONDS; export type PendingAuthPayload = { userId: string; otpId: string }; const PENDING_TTL_SECONDS = 60 * 10; export async function createPendingAuthToken(payload: PendingAuthPayload): Promise<string> { return new SignJWT(payload as any) .setProtectedHeader({ alg: "HS256" }) .setIssuedAt() .setExpirationTime(`${PENDING_TTL_SECONDS}s`) .sign(getSecret()); } export async function verifyPendingAuthToken(token: string): Promise<PendingAuthPayload | null> { try { const { payload } = await jwtVerify(token, getSecret()); return payload as unknown as PendingAuthPayload; } catch { return null; } } export const pendingCookieName = "ahm_pending_auth"; export const pendingMaxAgeSeconds = PENDING_TTL_SECONDS;
